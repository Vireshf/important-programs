string = "aaabccba"
max = ''
for i in range(len(string)):
    for j in range(i+1,len(string)+1):
        val = string[i:j]
        #print(val)
        val1 = val[::-1]
        if val == val1:
            if len(val) > len(max):
                max = val
print(max)
           
